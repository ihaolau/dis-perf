region=cn-north-1
ak=
sk=
projectId=
endpoint=https://dis.cn-north-1.myhwclouds.com:20004
# 通道名称
stream_name=

# 启动多少个发送线程
thread_num=1
# 每个发送线程发送多少次请求
request_num=1
# 每个发送请求包含多少条消息
record_in_request=1
# 每条消息的长度(bytes)
record_length=10
# 每次调用上传下载接口Sleep间隔(ms)
sleep_time=0

# 消费端每次请求最大期望接收条数
consumer_limit=10000
# 消费起始位置 (-2从最旧数据开始消费; -1从最新数据开始消费; 其他值则从指定位置消费)
consumer_offset=-1
# 分区数量 (auto表示自动调用describe接口获取分区数量)
partition_num=auto
IS_DEFAULT_TRUSTED_JKS_ENABLED=false